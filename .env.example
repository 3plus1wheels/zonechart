# Django
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1

# Database — option 1: full connection URL (recommended for Neon)
DATABASE_URL=postgresql://username:password@ep-xxxx-xxxx.us-east-2.aws.neon.tech/neondb?sslmode=require

# Database — option 2: individual fields (used if DATABASE_URL is not set)
DB_NAME=neondb
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432

# CORS
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# Frontend — leave empty on Render (same-origin), set to backend URL only if deploying separately
REACT_APP_API_BASE_URL=

# API
API_PAGINATION_SIZE=10

# Parser (set to 1 to enable verbose parser debug output)
PARSER_DEBUG=0
